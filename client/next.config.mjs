/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['thecsrjournal.in', 'b.thumbs.redditmedia.com', 'a.thumbs.redditmedia.com'],
    },
};

export default nextConfig;
